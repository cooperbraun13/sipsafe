/**
 * BAC Display Component
 * 
 * Displays the current estimated BAC value.
 * 
 * Features:
 * - Large, prominent BAC number
 * - Color coding based on BAC range
 * - Visual indicator (progress bar or gauge)
 * - Legal limit indicator
 * 
 * Props:
 *   bac: Current BAC value (number)
 * 
 * Usage:
 *   <BACDisplay bac={0.08} />
 */

'use client'

// TODO: Implement BAC display with:
// - Large number display
// - Color coding (green/yellow/red based on range)
// - Visual indicator (progress bar, gauge, etc.)
// - Show legal limit (0.08) as reference
// - Format to 3 decimal places

export default function BACDisplay({ bac }: { bac: number }) {
  return (
    <div>
      {/* TODO: Implement BAC display */}
    </div>
  )
}

