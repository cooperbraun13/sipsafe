/**
 * Session Card Component
 * 
 * Displays information about a drinking session.
 * 
 * Features:
 * - Session start time
 * - Number of drinks consumed
 * - Current/peak BAC
 * - Session duration
 * - Link to view full session details
 * 
 * Props:
 *   session: Session object with id, started_at, drinks, peak_bac
 * 
 * Usage:
 *   <SessionCard session={session} />
 */

'use client'

// TODO: Implement session card with:
// - Display session metadata
// - Format dates and times
// - Show drink count
// - Show BAC information
// - Link to session detail page
// - Handle active vs completed sessions differently

export default function SessionCard({ session }: { session: any }) {
  return (
    <div>
      {/* TODO: Implement session card */}
    </div>
  )
}

