/**
 * Sign Up Form Component
 * 
 * Client component for new user registration.
 * 
 * Features:
 * - Zagmail email input (must be @zagmail.gonzaga.edu)
 * - Username input
 * - Weight input (kg)
 * - Height input (cm)
 * - Gender selection
 * - "Sign Up" button that sends OTP code
 * - Stores signup data for profile creation after verification
 * - Redirects to verify-code page
 * 
 * Usage:
 *   <SignUpForm />
 */

'use client'

// TODO: Implement signup form with:
// - All required fields (email, username, weight, height, gender)
// - Validation for all inputs
// - Supabase signInWithOtp call with metadata
// - Store signup data in sessionStorage for profile creation
// - Redirect to /verify-code after code sent
// - Loading and error states

export default function SignUpForm() {
  return (
    <div>
      {/* TODO: Implement signup form */}
    </div>
  )
}

